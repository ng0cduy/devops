apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "s3-sync-service.fullname" . }}
  labels:
    {{- include "s3-sync-service.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}

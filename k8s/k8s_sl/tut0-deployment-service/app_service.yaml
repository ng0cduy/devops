apiVersion: v1
kind: Service
metadata:
  name: front-counter
spec:
  type: ClusterIP      # Internal counter (only for staff inside the cluster)
  selector:
    job: pizza-maker   # Send orders to anyone with this label
  ports:
    - port: 80         # Counter port
      targetPort: 80   # Worker port